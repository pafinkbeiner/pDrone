# install with pip(3) install -r requirements.txt
python-dotenv
logging
flask
threading
json
time
#sudo apt update && sudo apt --yes --force-yes install dnsmasq hostapd python3-dev python3-pip && sudo pip3 install pyaccesspoint
PyAccessPoint
pigpio